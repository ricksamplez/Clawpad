# Clawpad

Clawpad is a lightweight, DM-only, session-scoped notebook for OpenClaw.

It stores explicit working state for the current session only.

It is not memory, not retrieval, not transcript storage, and not a context-engine replacement.
